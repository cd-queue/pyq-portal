# pyq-portal

## Firebase setup
1. Copy `firebase-config.example.js` to `firebase-config.local.js`.
2. Put your own Firebase Web API key and project values in `firebase-config.local.js`.
3. Run the app using a local server (not `file://`).

`firebase-config.local.js` is ignored by git, so your key is not committed.
